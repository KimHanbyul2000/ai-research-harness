# Claude 컨텍스트

> 이 파일은 AI 에이전트가 세션마다 읽는 규칙서다. **골격 템플릿**이므로 도메인 내용이
> 비어 있다 — 자기 프로젝트에 맞게 `<...>` 부분을 채워 쓴다.

## 프로젝트 개요

<한두 문장. 무엇을 다루는 프로젝트인가.>

## 전제

> **코드는 틀리면 멈추지만, 분석은 틀려도 숫자가 나온다.**

아래 모든 규칙이 이 문장에서 나온다. 속도보다 **"나중에 틀렸음을 확인할 수 있는 상태"**를
먼저 만든다.

### 데이터 출처를 먼저 확인한다

| | |
|---|---|
| **생성 데이터** (시뮬레이션 등) | 생성 규칙이 코드에 있다 → **"왜 이 값인가"를 원리까지 물을 수 있다.** 분석이 표면의 상관에서 멈추면 더 내려간다 |
| **실측 데이터** | 원인이 데이터 바깥에 있다 → "어떤 경향이 있나"까지만. **원인을 단정하지 않는다** |

<이 프로젝트의 데이터가 어느 쪽인지 적는다.> 생성 데이터라면, 값에 대한 질문은
**생성 코드까지 따라가 답한다** — 관측된 경향으로 대신하지 않는다.

---

## 1. 포맷마다 역할이 다르다

산출물의 형태가 매번 다르면 **무엇을 검토해야 하는지가 흔들린다.** 포맷을 결과가 아니라
지시로 쓴다. 상세: [`docs/포맷별_역할.md`](docs/포맷별_역할.md)

| 포맷 | 단계 | 역할 | 검토 |
|---|---|---|---|
| `.md` | 전 단계 | 규칙 · 설계 · 해석 · 용어 | 읽는다. **용어는 장부 대조 후 확정** |
| `.svg` | 설계 | 구조 · 개념 · 관계. **값을 담지 않는다** | 본다 |
| `.html` | 검증 | 데이터 **동적 탐색**. 자기완결 파일 하나 | 조작한다. **아직 결론 아님** |
| `.png` | 결론 | 확정된 결과. 논문 수준으로만 | **사람이 승인한다** |

- 순서는 `.md` → `.svg` → `.html` → `.png`. **거꾸로 가지 않는다.**
- `.gif`·동영상·노트북·바이너리 문서 포맷은 쓰지 않는다(이유는 상세 문서에).
- `.html`에서 본 것은 `.md`나 `.png`로 옮겨 적기 전까지 **기록이 아니다.**

---

## 2. `.md`는 용어를 확정한 뒤에 확정된다

AI가 쓴 문서에서 가장 자주 틀리는 것은 숫자가 아니라 **용어**다. 용어 오류는 검산이 안 된다.

**`.md`를 만든 직후 [`GLOSSARY.md`](GLOSSARY.md)를 연다.**

| 대조 결과 | 처리 |
|---|---|
| 장부에 있다 | 표준 용어로 통일 |
| 장부에 없다 | 사람에게 보고한다. **AI가 표준 용어를 스스로 정하지 않는다** |
| 장부의 말인데 뜻이 다르다 | 🔴 멈춘다. 장부의 "혼동 주의" 절을 먼저 고친다 |

- 정의가 안 끝난 말은 **'미정'으로 비워둔다.** 그럴듯한 말을 임시로 채우면 그대로 굳는다.
- **내부 약어를 외부 문서에 쓰지 않는다** — 보고서 · 발표자료 · 공개 저장소.
- 절차 상세: [`docs/용어장부_규칙.md`](docs/용어장부_규칙.md)

---

## 3. 승인 없이 다음 단계로 가지 않는다

한 번 시켰는데 세 단계가 진행되면, 중간에서 갈렸어야 할 판단이 **기본값으로 지나간다.**
상세: [`docs/승인게이트.md`](docs/승인게이트.md)

| | 게이트 | 통과 조건 |
|---|---|---|
| 1 | 설계 → 실행 | **`00_설계목표.md`가 없으면 `run.py`를 만들지 않는다** |
| 2 | 실행 → 시각화 | 무거운 실행은 `--check-only`로 **점검을 먼저 보고**한다 |
| 3 | 시각화 → 확정 | **사람이 그림을 보고 승인**하기 전에 다음 분석을 시작하지 않는다 |
| 4 | 확정 → 추적 | [승격 관문](docs/승격기준.md) 두 개를 통과한다 |

**게이트를 늘리지 않는다.** 전부 물으면 사람이 전부 승인하게 되고, 그러면 게이트가 죽는다.

---

## 4. 오류가 나면 규칙을 한 줄 추가한다

같은 오류가 두 번 나면 실수가 아니라 구조 문제다. 고치고 끝내지 말고 **이 파일이나
`docs/`에 규칙을 한 줄 추가**한다. 규칙에는 **날짜와 실제 사고 내용**을 함께 적는다 —
이유를 모르는 규칙은 나중에 지워진다.

**시각화 오류는 [`docs/오답노트.md`](docs/오답노트.md)에 쌓는다.** 그림을 새로 설계하기
전에 읽는다. 기록 서식의 핵심 칸은 **"왜 안 걸렸나"** — 그 칸이 다음 검사를 설계하는 자리다.

> 특히 위험한 것은 **테스트를 통과하는 오류**다. 값은 맞는데 표현이 틀린 경우, 문자열
> 안에 있어 문법 검사가 닿지 않는 코드가 그렇다. 그런 층을 발견하면 검사가 닿는 형태로
> 꺼낸다(렌더 프로브 등).

**검출은 가능한 층부터 아래로 내린다:** ① 단일 출처 상수 → ② 그리는 코드와 계산 코드 분리
→ ③ 출력 테스트 → ④ 사람 확인. ④로 올라오는 것을 줄이는 게 목표다.

---

## 5. AI에게 맡기는 것과 맡기지 않는 것

| | |
|---|---|
| **사람이 정한다** | 분석 설계의 최초 제안 · 무엇을 측정할지 · 표준 용어 · 그림의 의도와 형식 · 승격 여부 |
| **AI가 한다** | 구현 · 검색 · 요약 · 문서화 · 대조 · 기록 강제 |
| **규칙과 테스트가 한다** | 검증 |

- 사람이 "무엇을 측정해야 하나"를 물으면 **답을 바로 나열하지 않는다.** 먼저 초안을
  받고, 그것을 검토·반박·보완하는 역할로 응답한다.
- AI가 그림의 **종류를 임의로 늘리지 않는다.** 왜 필요한지 먼저 설명하고 판단을 받는다.
- 근거가 없으면 **"근거 없음"을 먼저 밝힌다.** 그럴듯한 설명으로 빈틈을 메우지 않는다.
- 수치·기전 주장은 **1차 출처(원문·원본 데이터·코드)로 검증한 뒤** 말한다.

---

## 6. 작업 단위 = 분석 폴더 하나

모든 분석은 `out/YYYYMMDD_<주제>/` 폴더 하나로 격리한다. 완료하면 폴더째 `archive/`로
옮긴다. 두 폴더 모두 **git이 추적하지 않는다.**

```
out/YYYYMMDD_<주제>/
├── 00_설계목표.md   ← 실행 '전'. 이게 없으면 run.py 를 만들지 않는다 (게이트 1)
├── run.py           ← 조립만. 재사용 계산은 lib/ 에서 import
├── 01_결과해석.md   ← 실행 '후'
├── data/  figures/  ← 산출물 (추적 안 함)
```

**서식은 `templates/`에, 규칙은 [`docs/분석폴더_규칙.md`](docs/분석폴더_규칙.md)에,
채워진 예시는 `examples/`에 있다.**

### 결과해석에 반드시 남기는 것

1. **탈락 사유를 단계별로 분해** — 통과분만 적지 않는다
2. **커버리지 감사** — 판정 기준이 바뀌었으면 어디까지 재적용했는지 표로
3. **취약성** — 결론이 소수 데이터포인트에 걸려 있는지
4. **재현 검산** — 옛 기준으로 이전 결론이 나오는지 `assert`

---

## 7. git 추적은 3단이다

| 층 | 위치 | git | 성격 |
|---|---|---|---|
| 1. 작업공간 | `out/` → `archive/` | 추적 안 함 | 자유롭게 만들고 버린다 |
| 2. 스냅샷 | `bootstrap/<분석명>/` | 추적함 | 드라이버 **사본만**. 데이터는 안 담는다 |
| 3. 라이브러리 | `lib/`, `tests/` | 추적함 | 승격 통과분만 |

**승격 관문 — 둘 다 만족해야 한다** (상세: [`docs/승격기준.md`](docs/승격기준.md))

1. **의존성 없음** — `out/`·`archive/`·`bootstrap/` 파일에 의존하지 않게 만들 수 있다
2. **두 번째 사용자 있음** — 두 번째 분석이 실제로 같은 로직을 필요로 할 때 승격한다

**라이브러리는 안 늘어나는 것이 정상이다.** 빠르게 커지고 있으면 기준이 느슨해진 것이다.

---

## 8. 회귀 테스트

```bash
pytest tests -q
```

코드를 고쳤으면 돌린다. **실패하면 테스트를 고치기 전에 코드를 의심한다.** 기준값을
의도적으로 바꿨다면 왜 바뀌었는지 커밋 메시지에 적는다 — 이유를 모르는 값 변경은 회귀다.

---

## 9. 문서 규칙

- 폐기한 것은 **본문에서 제거**한다. 요약도 남기지 않는다 — 상세는 git 이력으로 복원한다.
- 같은 사실을 두 곳에 적지 않는다. **단일 출처**를 정하고 나머지는 링크한다.
- 점검 경위·버전 이력·정정 서술은 문서에 넣지 않는다.
