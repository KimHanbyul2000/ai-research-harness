# 용어 장부 (Glossary)

> **이 파일은 문서를 쓰기 전이 아니라 쓴 직후에 연다.** AI가 만든 `.md`는 여기 있는 표준
> 용어로 바꿔 확정한 뒤에야 확정 문서가 된다. 절차는 [`docs/용어장부_규칙.md`](docs/용어장부_규칙.md).
>
> 아래 항목은 **서식을 보여주기 위한 예시**다. 값·용어는 전부 가상이다.
> 자기 분야 용어로 갈아끼워 쓴다.

---

## 1. 표준 용어

각 항목은 네 칸이다. **"쓰지 않는 표현" 칸이 실제로 일을 한다** — 정의만 적어두면 AI는
동의어를 계속 만들어낸다.

| 표준 용어 | 정의 | 쓰지 않는 표현 | 확정 |
|---|---|---|---|
| **조건 배율** (condition scale) | 기준 조건 대비 입력 세기의 곱셈 계수. 무차원 | 강도, 가중치, 증폭률, 게인 | 2026-01-05 |
| **적용 시점** (onset) | 배율이 적용되기 시작하는 시각(ms). 구간의 시작이지 중심이 아니다 | 시작점, 개시 시간, 타이밍 | 2026-01-05 |
| **판정 구간** (decision window) | 대상을 통과/탈락으로 가르는 시간 범위. 경계는 양끝 포함 | 분석 창, 측정 윈도우, 타임 윈도우 | 2026-01-12 |
| **전수 재적용** (full re-application) | 기준 변경을 **대상 전체**에 다시 적용하는 것. 부분집합 재적용은 이 말을 쓰지 않는다 | 재분석, 재계산, 업데이트 | 2026-01-20 |

### 표준 용어가 아직 없는 것

정의가 안 끝난 말은 **비워두고 비어 있다고 적는다.** 빈 칸은 AI에게 "여기는 네가 채우지
말라"는 신호다. 임시로 그럴듯한 용어를 채워두면 그 말이 그대로 굳는다.

| 임시 표현 | 상태 |
|---|---|
| "경계 근처 대상" | 미정 — 경계에서 몇 이내를 말하는지 정하지 않았다 (2026-01-20~) |

---

## 2. 🔴 혼동 주의 — 같은 말이 다른 뜻인 곳

**가장 비싼 오류가 여기서 나온다.** 용어가 없어서가 아니라, **있는데 두 뜻으로 쓰여서**
생긴다. 아래 유형은 실제로 문서 세 곳에서 서로 다른 숫자를 내게 했다.

### 예시 — 배율을 가리키는 말 네 가지

| 말 | 실제로 가리키는 것 |
|---|---|
| 설정 배율 | 코드에 **입력한** 값 |
| 적용 배율 | 계산에 **실제로 쓰인** 값 (경계 처리 후) |
| 관측 배율 | 결과에서 **역산한** 값 |
| 전달 배율 | 다음 단계로 **넘어간** 값 |

**특정 설정에서는 이 넷이 전부 같은 값이 된다.** 그래서 평소엔 구분하지 않고 쓰다가,
조건이 바뀌어 값이 갈리는 순간 문서마다 다른 숫자가 나온다. 어느 것이 틀렸는지가 아니라
**어느 뜻으로 쓴 것인지**를 먼저 물어야 한다.

> **인용 지침:** 문서에서 배율을 인용할 때는 네 가지 중 **무엇인지 반드시 함께 적는다.**
> "배율 1.5"는 그 자체로 불완전한 서술이다.

---

## 3. 내부 약어

내부에서만 쓰는 약어는 **외부로 나가는 문서에 쓰지 않는다** — 보고서, 발표자료, 공개
저장소 README가 전부 해당한다. 외부 독자는 내부 약어를 모른다.

| 내부 약어 | 표준 용어 | 외부 문서 |
|---|---|---|
| (예시) `CSF` | 조건 배율 계수 (condition scale factor) | 표준 용어만 사용 |

- **내부 노트:** 약어를 써도 되지만 **첫 등장 시 표준 용어를 병기**한다.
- **외부 문서:** 약어 금지.
- 새 약어가 생기면 이 표에 먼저 추가한 뒤 쓴다.

---

## 4. 표기 규약

정의가 아니라 **쓰는 방식**을 고정하는 칸이다. 정의가 같아도 표기가 흔들리면 문서를 합칠 때
숫자가 섞인다.

| 대상 | 규약 | 이유 |
|---|---|---|
| 시각 | ms 단위, 정수 | 단위를 문서마다 바꾸면 비교가 깨진다 |
| 구간 경계 | 양끝 포함 `[a, b]` | 한쪽만 포함하는 표기와 섞이면 경계 대상 하나가 갈린다 |
| 번호 축 | 0부터 | 1부터 세는 문서와 섞이면 대상이 한 칸씩 밀린다 |
| 조건 이름 | 코드의 식별자 그대로 | 한글 별칭을 문서에서만 쓰면 grep이 안 닿는다 |

## --- Notes ---

- 이 파일은 **한 프로젝트에 하나**다. 문서마다 용어를 정의하면 그 정의들이 서로 어긋난다.
- 항목을 추가할 때는 **확정 날짜**를 같이 적는다. 언제부터 그 말이 그 뜻이었는지가
  과거 문서를 읽을 때 필요하다.
- 항목을 **고칠** 때는 그 용어를 쓴 기존 문서를 함께 확인한다 — 절차는
  [`docs/용어장부_규칙.md`](docs/용어장부_규칙.md)의 "용어가 바뀌었을 때".
